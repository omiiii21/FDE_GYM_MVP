# Copy this file to .env.local for local development
# For production (Vercel), set VITE_API_URL in the Vercel project dashboard

# URL of the deployed FastAPI backend (Render)
VITE_API_URL=https://your-render-service-name.onrender.com
